# Example GraphQL with Apollo Server

This a simple implementation graphql with Apollo Server in Typescript nodejs.

Open the terminal and execute this command to run example server
```
npm run start
```

Restu Muzakir